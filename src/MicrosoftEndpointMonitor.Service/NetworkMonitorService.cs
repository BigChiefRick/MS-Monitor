using Microsoft.Extensions.Hosting;
using Microsoft.Extensions.Logging;

namespace MicrosoftEndpointMonitor.Service
{
    // This file is replaced by the BackgroundService in Program.cs
    // Keeping for compatibility but the actual service logic is in Program.cs
}
